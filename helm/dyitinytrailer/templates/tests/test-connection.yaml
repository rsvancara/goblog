apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dyitinytrailer.fullname" . }}-test-connection"
  labels:
    {{- include "dyitinytrailer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dyitinytrailer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
