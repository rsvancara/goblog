apiVersion: v1
kind: Service
metadata:
  name: {{ include "dyitinytrailer.fullname" . }}
  labels:
    {{- include "dyitinytrailer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dyitinytrailer.selectorLabels" . | nindent 4 }}
