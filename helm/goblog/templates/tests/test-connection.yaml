apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goblog.fullname" . }}-test-connection"
  labels:
    {{- include "goblog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goblog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
